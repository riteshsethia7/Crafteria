import { ExternalDirective } from './external.directive';

describe('ExternalDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalDirective();
    expect(directive).toBeTruthy();
  });
});
